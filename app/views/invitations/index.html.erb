<div class="max-w-6xl mx-auto px-4 py-8">
  <!-- Page Header -->
  <div class="bg-white rounded-xl shadow-sm p-6 mb-6">
    <div class="flex items-center gap-3">
      <div class="w-12 h-12 rounded-full flex items-center justify-center text-white font-semibold text-xl" style="background-color: #228B22;">
        ✉️
      </div>
      <div>
        <h1 class="text-2xl font-bold text-gray-900">Gerenciar Convites</h1>
        <p class="text-gray-600">Convide novos jogadores para o FutSim</p>
      </div>
    </div>
  </div>

  <div class="grid md:grid-cols-2 gap-6">
    <!-- Invitation Form -->
    <div class="bg-white rounded-xl shadow-sm p-6">
      <div class="flex items-center gap-2 mb-6">
        <span class="text-xl">➕</span>
        <h2 class="text-xl font-semibold text-gray-900">Convidar Novo Usuário</h2>
      </div>

      <%= form_with url: invitations_path, method: :post, local: true do |form| %>
        <div class="mb-4">
          <%= form.label :email, "Endereço de Email", class: "block text-gray-700 text-sm font-semibold mb-2" %>
          <%= form.email_field :email, required: true,
              placeholder: "usuario@email.com",
              class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:border-transparent transition-all duration-200",
              style: "focus:ring-color: #228B22;" %>
        </div>

        <%= form.submit "Enviar Convite",
            class: "w-full px-4 py-3 rounded-lg font-medium text-white hover:opacity-90 transition-all duration-200 flex items-center justify-center gap-2",
            style: "background-color: #228B22;" do %>
          <span>📧</span>
          Enviar Convite
        <% end %>
      <% end %>

      <!-- Info Box -->
      <div class="mt-6 p-4 rounded-lg border-l-4 border-blue-500 bg-blue-50">
        <div class="flex items-center gap-2">
          <span class="text-blue-500">ℹ️</span>
          <h3 class="font-semibold text-blue-800">Como funciona</h3>
        </div>
        <p class="text-sm text-blue-700 mt-1">
          O usuário convidado poderá fazer login usando sua conta Google após receber o convite.
        </p>
      </div>
    </div>

    <!-- Pending Invitations -->
    <div class="bg-white rounded-xl shadow-sm p-6">
      <div class="flex items-center justify-between mb-6">
        <div class="flex items-center gap-2">
          <span class="text-xl">⏳</span>
          <h2 class="text-xl font-semibold text-gray-900">Convites Pendentes</h2>
        </div>
        <span class="px-3 py-1 rounded-full text-sm font-medium bg-gray-100 text-gray-700">
          <%= @invited_users.count %> convites
        </span>
      </div>

      <% if @invited_users.any? %>
        <div class="space-y-3">
          <% @invited_users.each do |user| %>
            <div class="border border-gray-200 rounded-lg p-4 hover:border-green-500 hover:shadow-md transition-all duration-200">
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 rounded-full flex items-center justify-center text-white font-semibold" style="background-color: #0047AB;">
                    <%= user.email.first.upcase %>
                  </div>
                  <div>
                    <div class="font-semibold text-gray-900"><%= user.email %></div>
                    <div class="text-sm text-gray-600">
                      Convidado em <%= user.invitation_created_at&.strftime("%d/%m/%Y às %H:%M") %>
                    </div>
                  </div>
                </div>

                <div class="flex items-center gap-2">
                  <span class="px-2 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-700">
                    Pendente
                  </span>
                  <%= link_to invitation_path(user), method: :delete,
                      confirm: "Tem certeza que deseja revogar este convite?",
                      class: "p-2 text-red-600 hover:bg-red-50 rounded-lg transition-all duration-200" do %>
                    <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                    </svg>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-8">
          <div class="w-16 h-16 mx-auto mb-4 rounded-full bg-gray-100 flex items-center justify-center">
            <span class="text-2xl text-gray-400">📭</span>
          </div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Nenhum convite pendente</h3>
          <p class="text-gray-600">Convide alguém para começar a jogar!</p>
        </div>
      <% end %>

      <!-- Stats -->
      <div class="mt-6 p-4 rounded-lg" style="background-color: #f8fafc;">
        <h3 class="font-semibold text-gray-900 mb-2">📊 Estatísticas de Convites</h3>
        <div class="grid grid-cols-2 gap-4">
          <div class="text-center">
            <div class="text-2xl font-bold" style="color: #228B22;"><%= @invited_users.count %></div>
            <div class="text-xs text-gray-600">Pendentes</div>
          </div>
          <div class="text-center">
            <div class="text-2xl font-bold text-blue-600">1</div>
            <div class="text-xs text-gray-600">Ativo</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>